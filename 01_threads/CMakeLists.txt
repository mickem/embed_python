CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(01_threads)

SET(SOURCE_FILES
	threads.cpp
)

IF(WIN32)
	SET(SOURCE_FILES ${SOURCE_FILES}
		threads.hpp
	)
ENDIF(WIN32)

ADD_EXECUTABLE(threads ${SOURCE_FILES})
TARGET_LINK_LIBRARIES(threads ${Boost_THREAD_LIBRARY})
